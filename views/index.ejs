<!DOCTYPE html>
<html>
    <head>
        <% include ./part/header %>
    </head>
    <body>
        <table id="table">
            <thead>
                <tr>
                    <th>NO</th>
                    <th>TITLE</th>
                    <th>ID</th>
                    <th>DATE</th>
                    <th>HIT</th>
                </tr>
            </thead>
            <tbody>
                <% print.forEach(function (board) { %>
                    <tr>
                        <td><%= board.no %></td>
                        <td><%= board.title %></td>
                        <td><%= board.id %></td>
                        <td><%= board.date %></td>
                        <td><%= board.hit %></td>
                    </tr>
                <% }) %>
            </tbody>
        </table>      
        <a href="./create">WRITE</a>
    </body>
</html>